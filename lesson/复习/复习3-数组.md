# 定长数组
```sol
uint[5] public numbers;
```
* 不支持push
* 不支持pop
* delete删除某个元素，这个元素变成0，length不变

## 动态数组
```sol
uint[] public numbers;
```
* 支持push
* 删除最后元素（pop）
* delete numbers--清空数组，length变成0













































