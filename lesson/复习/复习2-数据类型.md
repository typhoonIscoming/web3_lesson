## 值类型
bool、uint/int、address、bytes1-bytes32、enum

## 引用类型
array、string、struct、mapping、bytes

## unchecked
如果你确定不会溢出，可以使用unchecked来节省gas

## 地址类型
一个地址是20字节（160位）的值

## address payable

## 安全的类型转换
在进行显式转换时，应该先检查范围
**type(T).max和type(T).min：**

```sol
type(uint8).max,   // 255
type(uint8).min,   // 0
type(int8).max,    // 127
type(int8).min     // -128
```
























