## if

## else if 

## while

## do-while

## for

## break和continue

## 错误处理
1. require
2. assert
assert`assert(条件);`用于检查不应该失败的条件，主要用于检测代码bug和不变量。
* 交易回滚
* 表示代码有bug
* 返还剩余Gas（Solidity 0.8.0+）
* 不支持错误消息

3. revert
支持自定义错误
```sol
error Unauthorized(address caller); // 自定义错误更省Gas
revert Unauthorized(msg.sender);

需要错误处理？
    ↓
简单条件判断？
    ├─ 是 → require
    └─ 否 → 复杂逻辑？
            ├─ 是 → revert
            └─ 否 → 检查不变量？
                    └─ 是 → assert
```

## 状态机
状态机（State Machine）是一种管理复杂状态转换的设计模式
```sol
contract StateMachine {
    // 定义状态
    enum State {
        Preparing,   // 准备中
        Active,      // 进行中
        Checking,    // 检查中
        Success,     // 成功
        Failed,      // 失败
        Cancelled    // 已取消
    }
    
    State public currentState;
    
    // 状态检查modifier
    modifier inState(State expected) {
        require(currentState == expected, "Invalid state for this operation");
        _;
    }
    
    constructor() {
        currentState = State.Preparing;
    }
    
    // 只能在Preparing状态执行
    function start() public inState(State.Preparing) {
        currentState = State.Active;
    }
    
    // 只能在Active状态执行
    function contribute() public payable inState(State.Active) {
        // 贡献资金
    }
    
    // 只能在Active状态执行
    function check() public inState(State.Active) {
        currentState = State.Checking;
    }
    
    // 状态转换
    function finalize() public inState(State.Checking) {
        if (address(this).balance >= 100 ether) {
            currentState = State.Success;
        } else {
            currentState = State.Failed;
        }
    }
    
    // 紧急取消
    function cancel() public {
        require(
            currentState == State.Preparing || currentState == State.Active,
            "Cannot cancel at this stage"
        );
        currentState = State.Cancelled;
    }
}
```
```sol
[Preparing]
    │
    │ start()
    ↓
[Active] ────────┐
    │            │
    │ check()    │ contribute()
    ↓            │
[Checking] ──────┘
    │
    │ finalize()
    ├──────────┬──────────┐
    ↓          ↓          ↓
[Success] [Failed] [Cancelled]
```
状态机的优势：

* 状态转换清晰：明确哪些操作在哪些状态下可以执行
* 减少if-else嵌套：用状态替代复杂的条件判断
* 易于维护：添加新状态和转换很容易
* 防止无效操作：在错误状态下的操作会被拒绝
* 代码可读性高：状态名称清楚表达合约状态

## 安全编程原则
**Checks-Effects-Interactions模式**

这是智能合约开发中最重要的安全模式。































